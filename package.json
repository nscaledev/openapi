{
  "name": "nscaledev-openapi-pipeline",
  "private": true,
  "type": "module",
  "description": "Sanitize/validate scripts used by the publish-spec action to publish OpenAPI specs pushed here from other repos.",
  "scripts": {
    "test": "node --test scripts/__tests__/*.test.mjs"
  },
  "devDependencies": {
    "yaml": "^2.6.1"
  }
}
