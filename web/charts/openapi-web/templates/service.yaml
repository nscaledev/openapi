apiVersion: v1
kind: Service
metadata:
  name: {{ include "openapi-web.fullname" . }}
  labels:
    {{- include "openapi-web.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "openapi-web.labels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
